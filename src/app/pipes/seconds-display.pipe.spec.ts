import { SecondsDisplayPipe } from './seconds-display.pipe';

describe('SecondsDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondsDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
